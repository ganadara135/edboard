# 개발용 파일
.git*
# X 로 시작하는 파일과 폴더 
*/X*
*/X_*
# subdirectory 에서 X 로 시작하는 파일과 폴더 제외
*/*/X*
*.md
# Exclude files and directories in the root directory whose names are a one-character extension of temp. For example, /tempa and /tempb are excluded.
app?
db-data?
db_postgre?
testnodemailer?
dockerCompose?
app
db-data
db_postgre
testnodemailer
dockerCompose
