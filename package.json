{
    "private": true,
    "scripts": {
        "build:server": "lerna run build --scope={@abb/server,@abb/common}",
        "build:web": "lerna run build --scope={@abb/web,@abb/common}"
    },
    "workspaces": {
        "packages": [
            "packages/*"
        ],
        "nohoist": [
            "**/rimraf",
            "**/rimraf/**"
        ]
    },
    "dependencies": {
        "create-react-app": "^3.4.0"
    },
    "name": "abb",
    "devDependencies": {
        "lerna": "^3.20.2"
    }
}
